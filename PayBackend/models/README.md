# Models

This project uses Drogon ORM models generated from `model.json`.

Generate models with:

```
..\scripts\generate_models.bat
```

Then include the generated headers from `models/` in services as needed.
